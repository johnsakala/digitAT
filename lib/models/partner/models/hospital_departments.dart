class HospitalDepartment{
  int id;
  String name;
  int parentId;
  bool isSelected;
  String avatar;
  String categoryName;

  HospitalDepartment.add(this.name,this.isSelected, this.avatar,this.categoryName);
}